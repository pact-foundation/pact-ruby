Given **<%= interaction.provider_state %>**,
upon receiving **<%= interaction.description %>** from <%= interaction.consumer_name %>,
with
```json
<%= interaction.request  %>
```
<%= interaction.provider_name %> will respond with:
```json
<%= interaction.response %>
```
